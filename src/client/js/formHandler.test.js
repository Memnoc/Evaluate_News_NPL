import { sentimentFrontendData } from "./formHandler";

test('sentiment frontend data is not null', () => {
    expect(sentimentFrontendData).not.toBe(null);
});

test('classify frontend data is not null', () => {
    expect(sentimentFrontendData).not.toBe(null);
});